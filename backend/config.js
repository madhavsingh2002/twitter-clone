/* The code `module.exports` is used in Node.js to export a module or object from a file. In this case,
it is exporting an object with two properties: `MONGODB_URL` and `JWT_SECRET`. These properties hold
the values for a MongoDB connection URL and a JSON Web Token secret key, respectively. This allows
other files or modules to import and use these values. */
export const  MONGODB_URL = 'mongodb://0.0.0.0:27017/twitter-clone';
export const JWT_SECRET = "hafsfffgg72846285jjjkh54646467hh";